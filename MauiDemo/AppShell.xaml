<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiDemo.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiDemo"
    xmlns:pages="clr-namespace:MauiDemo.Resources.Pages"
    Shell.FlyoutBehavior="Disabled">
    <FlyoutItem>
        <Tab Title="First">
            <ShellContent Title="Grid" ContentTemplate="{DataTemplate pages:GridPage}" />
            <ShellContent Title="Stack" ContentTemplate="{DataTemplate pages:StackPage}" />
        </Tab>
    </FlyoutItem>

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
